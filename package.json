{
  "name": "dormdesigner",
  "version": "1.0.0",
  "description": "Each subteam (Frontend, Backend, 3D) has their own branch and consists of 2 members. Each person maintains their own personal branch when developing and pushes only to their subteam's branch. All merge conflicts must be resolved at this level before proceeding.",
  "main": "back-end/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    ".": "cd dorm-designer && npm run build && cd .. && node .",
    "dev set up": "npm i && cd dorm-designer && npm install && npm i react-router-dom && npm i react-scripts && cd .. && cd back-end && npm install && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongodb": "^6.10.0",
    "mongoose": "^8.7.2",
    "react-router-dom": "^6.28.0",
    "react-scripts": "^5.0.1"
  },
  "type": "module",
  "keywords": [],
  "devDependencies": {
    "cypress": "^13.16.1"
  }
}
